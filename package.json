{
  "name": "job-portal-root",
  "version": "1.0.0",
  "description": "Orchestrator for Job Portal full-stack application.",
  "private": true,
  "scripts": {
    "install:all": "concurrently \"npm install --prefix frontend\" \"npm install --prefix backend\"",
    "seed": "node backend/seed.js",
    "dev": "concurrently \"npm run start --prefix frontend\" \"npm run dev --prefix backend\"",
    "start": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
